<Window x:Class="Translator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Translate" Width="1100" Height="820" MinWidth="900" MinHeight="650"
        WindowStartupLocation="CenterScreen" Background="White"
        FontFamily="Segoe UI" FontSize="14" Foreground="#20252B">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="LineBrush" Color="#E4E7EB"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#68717C"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#20252B"/>
            <Setter Property="BorderBrush" Value="#D7DCE2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.8"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.65"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D7DCE2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="SelectedIndex" Value="0"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#515A66"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,7"/>
        </Style>
        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style x:Key="SectionHeading" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        <Style x:Key="SettingsGroup" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="9"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        <Style x:Key="Message" TargetType="Border">
            <Setter Property="Background" Value="#F6F7F9"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="9"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,64,20"/>
        </Style>
        <Style x:Key="MyMessage" TargetType="Border" BasedOn="{StaticResource Message}">
            <Setter Property="Background" Value="#E8F1FF"/>
            <Setter Property="BorderBrush" Value="#B8D2FF"/>
        </Style>
        <Style x:Key="OtherMessage" TargetType="Border" BasedOn="{StaticResource Message}">
            <Setter Property="Background" Value="#FFF3E6"/>
            <Setter Property="BorderBrush" Value="#FFD1A3"/>
            <Setter Property="Margin" Value="64,0,0,20"/>
        </Style>
        <Style x:Key="Translation" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="0,12,0,0"/>
        </Style>
        <Style x:Key="MyTranslation" TargetType="Border" BasedOn="{StaticResource Translation}">
            <Setter Property="Background" Value="#D8E7FF"/>
            <Setter Property="BorderBrush" Value="#5B8DEF"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
        </Style>
        <Style x:Key="OtherTranslation" TargetType="Border" BasedOn="{StaticResource Translation}">
            <Setter Property="Background" Value="#FFE2C2"/>
            <Setter Property="BorderBrush" Value="#E58A24"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="34"/>
        </Grid.RowDefinitions>
        <!-- Shared header stays visible on all screens. -->
        <Border Grid.Row="0" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" Padding="28,20">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#EDF3FF" CornerRadius="10" Width="44" Height="44" Margin="0,0,12,0">
                        <TextBlock Text="A↔" Foreground="{StaticResource AccentBrush}" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Translate" FontSize="19" FontWeight="SemiBold"/>
                        <TextBlock Text="Every voice, understood." Style="{StaticResource MutedText}" Margin="0,3,0,0"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Ellipse x:Name="SessionStatusDot" Width="7" Height="7" Fill="#0BDA51" Margin="0,0,7,0"/>
                    <TextBlock x:Name="SessionStatusText" Text="Ready to set up" Style="{StaticResource MutedText}" Foreground="#20252B" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <Border BorderThickness="1" CornerRadius="7" Padding="12,2">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#E6F4F1"/>
                                <Setter Property="BorderBrush" Value="#2A9D8F"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ConfidentialModeCheckBox}" Value="True">
                                        <Setter Property="Background" Value="#147D72"/>
                                        <Setter Property="BorderBrush" Value="#0F665E"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <CheckBox x:Name="ConfidentialModeCheckBox" Content="Confidential mode" FontSize="12" FontWeight="SemiBold">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                    <Setter Property="Foreground" Value="#176B61"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Static screens only. Change Visibility later when implementing navigation. -->
        <Grid x:Name="WelcomeView" Grid.Row="1" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="420">
                <Border Background="#EDF3FF" CornerRadius="18" Width="80" Height="80" Margin="0,0,0,24">
                    <TextBlock Text="A↔" FontSize="32" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Let’s start a conversation." FontSize="26" FontWeight="SemiBold" TextAlignment="Center" Margin="0,0,0,16"/>
                <TextBlock Text="Choose your languages, voices and audio devices before you begin." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" TextAlignment="Center" LineHeight="24" Margin="0,0,0,28"/>
                <Button x:Name="StartTranslateButton" Content="Start translate" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Center" Padding="26,12"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="ConversationView" Grid.Row="1" Visibility="Visible">
            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="72"/></Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="250"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,1,0" Padding="22,26">
                    <StackPanel>
                        <TextBlock Text="Your session" Style="{StaticResource SectionHeading}"/>
                        <TextBlock Text="My translated voice" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="QuickMyVoiceComboBox" Margin="0,0,0,18">
                            <ComboBoxItem Content="Thorsten · Male"/><ComboBoxItem Content="Katja · Female"/><ComboBoxItem Content="Automatic · Match language"/>
                        </ComboBox>
                        <TextBlock Text="Their translated voice" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="QuickTheirVoiceComboBox">
                            <ComboBoxItem Content="HFC · Female"/><ComboBoxItem Content="Alan · Male"/><ComboBoxItem Content="Automatic · Match language"/>
                        </ComboBox>
                        <Border Height="1" Background="{StaticResource LineBrush}" Margin="0,24,0,14"/>
                        <CheckBox x:Name="QuickMyTranscriptCheckBox" Content="My transcript" IsChecked="True"/>
                        <CheckBox x:Name="QuickTheirTranscriptCheckBox" Content="Other people’s transcript" IsChecked="True"/>
                        <Button x:Name="SettingsButton" Content="Settings" Margin="0,24,0,0" Click="SettingsButton_Click" Background="#E8F1FF" Foreground="#1D4ED8" BorderBrush="#93B4F5"/>
                    </StackPanel>
                </Border>
                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="28,26">
                        <Grid Margin="0,0,0,24">
                            <StackPanel><TextBlock Text="Conversation" Style="{StaticResource SectionHeading}" Margin="0,0,0,5"/><TextBlock x:Name="ConversationDirectionText" Text="You: English → German · Others: German → English" Style="{StaticResource MutedText}"/></StackPanel>
                            <TextBlock Text="Sample transcript" FontSize="11" Foreground="#7B828B" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                        </Grid>
                        <StackPanel x:Name="TranscriptPanel">
                            <Border x:Name="MyFirstTranscript" Style="{StaticResource MyMessage}">
                                <StackPanel>
                                    <Grid Margin="0,0,0,8"><TextBlock Text="You · Original" Style="{StaticResource MutedText}"/><TextBlock Text="10:42:08" Style="{StaticResource MutedText}" HorizontalAlignment="Right"/></Grid>
                                    <TextBlock x:Name="LatestSourceText" Text="Waiting for speech…" TextWrapping="Wrap"/>
                                    <Border Style="{StaticResource MyTranslation}"><StackPanel><TextBlock x:Name="LatestTranslationLabel" Text="TRANSLATION" FontSize="11" FontWeight="SemiBold" Foreground="#315C9B" Margin="0,0,0,4"/><TextBlock x:Name="LatestTranslationText" Text="The translation will appear here." TextWrapping="Wrap"/></StackPanel></Border>
                                </StackPanel>
                            </Border>
                            <Border x:Name="TheirTranscript" Style="{StaticResource OtherMessage}">
                                <StackPanel>
                                    <Grid Margin="0,0,0,8"><TextBlock Text="Other person · Original" Style="{StaticResource MutedText}"/><TextBlock Text="10:42:15" Style="{StaticResource MutedText}" HorizontalAlignment="Right"/></Grid>
                                    <TextBlock x:Name="LatestTheirSourceText" Text="Waiting for the other person…" TextWrapping="Wrap"/>
                                    <Border Style="{StaticResource OtherTranslation}"><StackPanel><TextBlock x:Name="LatestTheirTranslationLabel" Text="TRANSLATION" FontSize="11" FontWeight="SemiBold" Foreground="#8A4D0F" Margin="0,0,0,4"/><TextBlock x:Name="LatestTheirTranslationText" Text="Their translation will appear here." TextWrapping="Wrap"/></StackPanel></Border>
                                </StackPanel>
                            </Border>
                            <Border x:Name="MySecondTranscript" Style="{StaticResource MyMessage}">
                                <StackPanel>
                                    <Grid Margin="0,0,0,8"><TextBlock Text="You · Original" Style="{StaticResource MutedText}"/><TextBlock Text="10:42:22" Style="{StaticResource MutedText}" HorizontalAlignment="Right"/></Grid>
                                    <TextBlock Text="Friday works for our team." TextWrapping="Wrap"/>
                                    <Border Style="{StaticResource MyTranslation}"><StackPanel><TextBlock Text="TRANSLATION · GERMAN" FontSize="11" FontWeight="SemiBold" Foreground="#315C9B" Margin="0,0,0,4"/><TextBlock Text="Freitag passt für unser Team." TextWrapping="Wrap"/></StackPanel></Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        <StackPanel x:Name="HiddenTranscriptsView" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,90,0,0">
                            <TextBlock Text="Conversation in progress" FontSize="18" FontWeight="SemiBold" TextAlignment="Center"/>
                            <TextBlock Text="Transcripts are hidden. Turn them on in the side panel." Style="{StaticResource MutedText}" TextAlignment="Center" Margin="0,12,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
            <Border Grid.Row="1" Background="#FAFBFC" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,1,0,0" Padding="28,15">
                <Grid><Button x:Name="StopTranslationButton" Content="Stop translation" HorizontalAlignment="Left" Background="#DC2626" Foreground="White" BorderBrush="#B91C1C"/><TextBlock x:Name="SessionDirectionText" Text="English → German · German → English" Style="{StaticResource MutedText}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid>
            </Border>
        </Grid>

        <Grid x:Name="SettingsView" Grid.Row="1" Visibility="Collapsed">
            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="72"/></Grid.RowDefinitions>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="28,26">
                    <Grid Margin="0,0,0,26">
                        <StackPanel><TextBlock Text="Settings" FontSize="26" FontWeight="SemiBold"/><TextBlock Text="Set up both sides of your conversation." Style="{StaticResource MutedText}" Margin="0,6,0,0"/></StackPanel>
                        <Button x:Name="ResetSettingsButton" Content="Reset to defaults" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#EEF2F7" Foreground="#334155" BorderBrush="#94A3B8"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SettingsGroup}" VerticalAlignment="Top">
                            <StackPanel>
                                <TextBlock Text="My voice · You → other people" Style="{StaticResource SectionHeading}"/>
                                <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                    <StackPanel><TextBlock Text="I speak" Style="{StaticResource FieldLabel}"/><ComboBox x:Name="MyLanguageComboBox"><ComboBoxItem Content="English"/><ComboBoxItem Content="German"/><ComboBoxItem Content="Lithuanian"/></ComboBox></StackPanel>
                                    <StackPanel Grid.Column="2"><TextBlock Text="I want to be heard in" Style="{StaticResource FieldLabel}"/><ComboBox x:Name="MyTargetLanguageComboBox"><ComboBoxItem Content="German"/><ComboBoxItem Content="English"/><ComboBoxItem Content="Lithuanian"/></ComboBox></StackPanel>
                                </Grid>
                                <TextBlock Text="My translated voice" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="MyVoiceComboBox" Margin="0,0,0,16"><ComboBoxItem Content="Thorsten · Male"/><ComboBoxItem Content="Katja · Female"/><ComboBoxItem Content="Automatic · Match language"/></ComboBox>
                                <Grid><TextBlock Text="Voice speed" Style="{StaticResource FieldLabel}"/><TextBlock Text="1.0×" Style="{StaticResource FieldLabel}" HorizontalAlignment="Right"/></Grid>
                                <Slider x:Name="MyVoiceSpeedSlider" Minimum="0.5" Maximum="1.5" Value="1" TickFrequency="0.1" IsSnapToTickEnabled="True" Margin="0,0,0,18"/>
                                <TextBlock Text="My microphone · Audio source" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="MyMicrophoneComboBox" Margin="0,0,0,16"><ComboBoxItem Content="Automatic · Default microphone"/><ComboBoxItem Content="Microphone · USB headset"/><ComboBoxItem Content="Microphone · Laptop"/></ComboBox>
                                <TextBlock Text="Send my translated audio to" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="MyAudioOutputComboBox" Margin="0,0,0,16"><ComboBoxItem Content="CABLE Input · Virtual playback"/><ComboBoxItem Content="Default speakers"/><ComboBoxItem Content="Headphones · USB headset"/></ComboBox>
                                <TextBlock Text="Meeting app microphone" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="MeetingMicrophoneComboBox" Margin="0,0,0,12"><ComboBoxItem Content="CABLE Output · Virtual capture"/><ComboBoxItem Content="Default microphone"/></ComboBox>
                                <TextBlock Text="Select this microphone in Teams, Zoom or your meeting app." Style="{StaticResource MutedText}"/>
                            </StackPanel>
                        </Border>
                        <StackPanel Grid.Column="2">
                            <Border Style="{StaticResource SettingsGroup}"><StackPanel>
                                <TextBlock Text="Other people · Other people → you" Style="{StaticResource SectionHeading}"/>
                                <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                    <StackPanel><TextBlock Text="They speak" Style="{StaticResource FieldLabel}"/><ComboBox x:Name="TheirLanguageComboBox"><ComboBoxItem Content="German"/><ComboBoxItem Content="English"/><ComboBoxItem Content="Lithuanian"/></ComboBox></StackPanel>
                                    <StackPanel Grid.Column="2"><TextBlock Text="Translate for me into" Style="{StaticResource FieldLabel}"/><ComboBox x:Name="TheirTargetLanguageComboBox"><ComboBoxItem Content="English"/><ComboBoxItem Content="German"/><ComboBoxItem Content="Lithuanian"/></ComboBox></StackPanel>
                                </Grid>
                                <TextBlock Text="Their translated voice" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="TheirVoiceComboBox" Margin="0,0,0,16"><ComboBoxItem Content="HFC · Female"/><ComboBoxItem Content="Alan · Male"/><ComboBoxItem Content="Automatic · Match language"/></ComboBox>
                                <Grid><TextBlock Text="Voice speed" Style="{StaticResource FieldLabel}"/><TextBlock Text="1.0×" Style="{StaticResource FieldLabel}" HorizontalAlignment="Right"/></Grid>
                                <Slider x:Name="TheirVoiceSpeedSlider" Minimum="0.5" Maximum="1.5" Value="1" TickFrequency="0.1" IsSnapToTickEnabled="True" Margin="0,0,0,18"/>
                                <TextBlock Text="Other people’s audio · Audio source" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="TheirAudioSourceComboBox" Margin="0,0,0,16"><ComboBoxItem Content="Meeting app audio · Application capture"/><ComboBoxItem Content="System audio · Loopback"/><ComboBoxItem Content="Virtual audio device"/></ComboBox>
                                <TextBlock Text="Hear their translated audio on" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="TheirAudioOutputComboBox"><ComboBoxItem Content="Headphones · Default output"/><ComboBoxItem Content="Headphones · USB headset"/><ComboBoxItem Content="Default speakers"/></ComboBox>
                            </StackPanel></Border>
                            <Border Style="{StaticResource SettingsGroup}"><StackPanel>
                                <TextBlock Text="Conversation preferences" Style="{StaticResource SectionHeading}" Margin="0,0,0,10"/>
                                <CheckBox x:Name="MyTranscriptCheckBox" Content="Show my transcript" IsChecked="True"/>
                                <CheckBox x:Name="TheirTranscriptCheckBox" Content="Show other people’s transcript" IsChecked="True"/>
                                <CheckBox x:Name="SpeakMyTranslationCheckBox" Content="Speak my translation" IsChecked="True"/>
                                <CheckBox x:Name="SpeakTheirTranslationCheckBox" Content="Speak their translation" IsChecked="True"/>
                                <Expander Header="Advanced &amp; diagnostics" Margin="0,12,0,0"><StackPanel Margin="0,14,0,0">
                                    <TextBlock Text="Session profile" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox x:Name="SessionProfileComboBox" Margin="0,0,0,8"><ComboBoxItem Content="Custom conversation"/><ComboBoxItem Content="English ↔ German"/><ComboBoxItem Content="Lithuanian ↔ English"/></ComboBox>
                                    <CheckBox x:Name="CaptureDiagnosticsCheckBox" Content="Capture diagnostics"/>
                                </StackPanel></Expander>
                            </StackPanel></Border>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
            <Border Grid.Row="1" Background="#FAFBFC" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,1,0,0" Padding="28,15">
                <Grid><TextBlock Text="Apply your settings to enter the conversation." Style="{StaticResource MutedText}" VerticalAlignment="Center"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button x:Name="BackButton" Content="Back" Margin="0,0,10,0"/><Button x:Name="ApplySettingsButton" Content="Apply" Style="{StaticResource PrimaryButton}" MinWidth="100"/></StackPanel></Grid>
            </Border>
        </Grid>
        <Border Grid.Row="2" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,1,0,0" Padding="28,8">
            <TextBlock Text="Design preview · No audio is captured or translated." FontSize="11" Foreground="#7B828B"/>
        </Border>
    </Grid>
</Window>
